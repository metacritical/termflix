<?xml version="1.0" encoding="UTF-8"?>
<!--
  Search Input Layout
  Used as an FZF "input box" fallback when gum is not installed.
-->
<fzf-layout id="search-input" name="Search Input">
  <layout direction="reverse" height="8" margin="20%,25%" padding="1" info="inline"/>
  <border style="rounded" label=" [ Enter:Search | Esc:Back ] " label-pos="bottom"/>
  <prompt pointer="➜">Search &gt; </prompt>
  <header first="true">Type and press Enter</header>
  <colors theme="dynamic">
    <color name="fg" value="#ffffff"/>
    <color name="fg+" value="#ffffff"/>
    <color name="hl" value="${THEME_HEX_GLOW:-#e879f9}"/>
    <color name="hl+" value="${THEME_HEX_GLOW:-#e879f9}"/>
    <color name="pointer" value="${THEME_HEX_GLOW:-#e879f9}"/>
    <color name="border" value="${THEME_HEX_GLOW:-#e879f9}"/>
    <color name="prompt" value="${THEME_HEX_GLOW:-#e879f9}"/>
    <color name="info" value="${THEME_HEX_GLOW:-#e879f9}"/>
    <color name="bg+" value="${THEME_HEX_BG_SELECTION:-#374151}"/>
  </colors>
  <bindings>
    <bind key="esc" action="abort"/>
  </bindings>
</fzf-layout>
